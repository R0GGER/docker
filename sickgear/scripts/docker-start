#!/bin/bash
set -eo pipefail
trap "exit 0" 2 15

cd $SB_HOME
git checkout $SICKRAGE_CHANNEL
git up

# chown -Rc sickgear:sickgear /torrents /tv_shows /downloads /sickgear /data
exec -a "supervisord" "supervisord" "-c" "/etc/supervisord.conf"
exit 0
